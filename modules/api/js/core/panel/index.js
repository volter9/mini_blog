/**
 * mini_blog left side admin panel
 * 
 * Its responsibility is to display mods (buttons for editing),
 * status bar, and 
 * 
 * @package mini_blog
 */

module.exports = {
    add: require('./add')
};